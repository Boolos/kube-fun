language: node_js

node_js:
    - 7.4.0

build:
    ci:
        - docker build -t pmartin/hello-node:$BUILD_NUMBER .

    push:
        - docker push pmartin/hello-node:$BUILD_NUMBER

integrations:
    hub:
        - integrationName: pauls-docker-hub
          type: docker
