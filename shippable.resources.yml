resources:
    - name: app-image
      type: image
      integration: pauls-docker-hub
      pointer:
          sourceName: pmartin/hello-node
      seed:
          versionName: master.9

    - name: k8s-cluster
      type: cluster
      integration: pauls-k8s-cluster
